文件夹的中文注释为简要功能介绍，运行时请删除

需要在D盘创建文件夹models，并在文件夹内配置如下文件 路径要求为

PS D:\models> Get-DirectoryTree -Path "D:\models"

bert-base-uncased
├── config.json
├── pytorch_model.bin
├── tokenizer_config.json
└── vocab.txt

resnet
└── resnet152-394f9c45.pth

torch_hub
└── hub
    └── checkpoints

torchvision
└── vit_b_16.pth

vit-base-patch16-224
├── config.json
├── preprocessor_config.json
└── pytorch_model.bin

vit-large-patch16-224
├── config.json
├── preprocessor_config.json
└── pytorch_model.bin

否则需要魔法环境云端配置 效率低 慢



